{
  "name": "genealogical-index",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/sites/*"
  ],
  "scripts": {
    "dev:slo": "npm run dev --workspace=packages/sites/slo",
    "build:slo": "npm run build --workspace=packages/sites/slo",
    "preview:slo": "npm run preview --workspace=packages/sites/slo"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
