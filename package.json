{
  "name": "genealogical-index",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "sites/*"
  ],
  "scripts": {
    "dev:slo": "npm run dev --workspace=sites/slo",
    "build:slo": "npm run build --workspace=sites/slo",
    "preview:slo": "npm run preview --workspace=sites/slo",
    "dev:cro": "npm run dev --workspace=sites/cro",
    "build:cro": "npm run build --workspace=sites/cro",
    "preview:cro": "npm run preview --workspace=sites/cro"
  },
  "devDependencies": {
    "vite": "^8.0.9"
  }
}
