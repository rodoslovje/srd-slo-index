{
    # Global options
    # Uncomment while testing to avoid hitting Let's Encrypt rate limits:
    # acme_ca https://acme-staging-v02.api.letsencrypt.org/directory
}

# Import all per-service snippets from conf.d/
# To add a new service: drop a .caddyfile file in conf.d/ and reload Caddy.
import /etc/caddy/conf.d/*.caddyfile
